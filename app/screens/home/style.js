import { StyleSheet } from 'react-native';

export default StyleSheet.create({

  containerMain: {
    display: 'flex',
    height: '100%',
    width: '100%',
    justifyContent: 'center',
    alignItems: 'center'
  },

});
